# プロジェクト名

プロジェクトの概要を1行で記述する

# 構造

プロジェクトのディレクトリ構造を記述する

# 技術スタック

- 使用している言語・フレームワーク・ライブラリを列挙する

# 調査の順序（厳守）

コードベースを調査するときは **必ず** 以下の順序で進める。網羅的なコード走査を最初に行わないこと:

1. `docs/` — 仕様書で全体像・設計判断を把握
2. `CLAUDE.md` / `.claude/rules/` — 構成・ルールを確認
3. ソースコード — 上記で把握できない実装詳細だけを確認
